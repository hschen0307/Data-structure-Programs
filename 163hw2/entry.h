/*
#include <cstring>
#include <cctype>
#include <iostream>


class cust_entry
{
	public:
		
		cust_entry(void);
		cust_entry(void);
		int create_entry(char * name, int worst_time, int average_time);
		int copy_entry(const cust_entry & a_new_entry);
		int retrieve(char * name, cust_entry & found) const;
		int retrieve(cust_entry & found) const;
		int display(void) const;

	private:
		char * name;
		char * order;
		int arrival_time;
		int long_time;
		int average_time;

};


*/
